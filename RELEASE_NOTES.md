# 群众投票 v0.3.3

本版本修复导出前真实打印预览灰屏，并把文字与打勾位置调整统一到真实 Word→PDF 预览中。

## 更新内容

- 修复真实打印预览首次打开只显示灰色背景的问题；现在会立即绘制 Word 直接导出的 PDF 页面。
- 主页面移除“打勾位置微调”，避免主页面预览与最终打印预览存在两套位置逻辑。
- 真实打印预览新增当前票面打勾调整：文字和打勾都支持鼠标拖拽、方向键 1pt、Shift 5pt、Ctrl 0.1pt。
- 每次停手后后台重新生成 Word→PDF 精确预览；刷新完成前不能确认导出。
- 兼容旧版打勾偏移数据，新调整统一按实际 pt 保存。
- 修复结果列按“结果/选项”组织时的打勾匹配，真实预览只列出当前记录实际出现的打勾。
- 预览、精确刷新、批量导出和更新检查统一使用线程安全的主界面事件队列；关闭窗口会取消残留轮询，避免卡死和 Tcl 回调冲突。
- 批量导出增加运行锁和配置快照，连续点击不会启动重复导出。
- 继续保留按模板内容自动保存和恢复判断区、标记区、字段位置及样式；已有模板标注不会因设置模板或重启而丢失。

## 下载与使用

下载 `qunzhong-toupiao.exe` 后直接运行。模板配置保存在本机 `%LOCALAPPDATA%\QunzhongVote\template_profiles.json`，模板、投票数据、配置和导出结果都不会上传到网络。
