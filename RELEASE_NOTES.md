# 群众投票 v0.3.1

本版本进一步增强真实打印预览中的文字位置调整。

## 更新内容

- 姓名、房号、电话等字段现在可直接按住文字或红框拖动。
- 点击预览后可使用键盘方向键移动当前文字：普通方向键每次 1pt，Shift 每次 5pt，Ctrl 每次 0.1pt。
- 拖动和键盘移动都会立即显示最新位置，停手后自动在后台生成精确的 Word→PDF 打印预览。
- 精确预览完成前暂不允许确认导出，避免实际打印位置与画面不一致。
- 界面按钮、状态提示和操作记录已统一为正式产品用语。
- 保留 GitHub Releases 更新检查，并在 GitHub API 限流时自动使用发布页面作为备用通道。

## 下载与使用

下载 `qunzhong-toupiao.exe` 后直接运行。真实打印预览优先调用 Microsoft Word；未安装 Word 时可使用 LibreOffice。

模板、投票数据和导出结果仅保存在本机，程序不会上传这些文件。
