# 群众投票 v0.3.2

本版本修复重新选择或设置模板后，判断区和标记区消失的问题。

## 更新内容

- 模板标注现在会自动保存在本机，重新点击“设置模板”不会再清空已有判断区、标记区、字段位置和样式。
- 重新选择同一份模板或重启程序后，会自动恢复该模板对应的全部标注。
- 模板配置按文件内容的 SHA-256 区分；模板内容发生变化后不会误套旧坐标，相同内容的模板副本可以继续使用原配置。
- 新增损坏配置保护、原子写入和多模板隔离，避免配置文件异常影响程序启动。
- 保留真实打印预览中文字拖拽和方向键移动功能：普通方向键每次 1pt，Shift 每次 5pt，Ctrl 每次 0.1pt。
- 完成模板标注、文字位置调整、验证规则修改和程序退出时都会自动保存当前模板设置。

## 下载与使用

下载 `qunzhong-toupiao.exe` 后直接运行。模板配置保存在本机 `%LOCALAPPDATA%\QunzhongVote\template_profiles.json`，模板、投票数据、配置和导出结果都不会上传到网络。
